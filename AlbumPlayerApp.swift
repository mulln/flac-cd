import SwiftUI

@main
struct AlbumPlayerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .frame(minWidth: 600, minHeight: 560)
        }
        .windowStyle(.automatic)
    }
}
